# Session Handoff — 2026-06-09

## Active Task

**Task:** Remove all hardcoded/mock data from UI components and replace with empty state scaffolding ready for real data integration.

**Phase:** Implementation — in progress, not yet committed.

**Checklist status:**
- [x] Identified all files with hardcoded mock data
- [x] Replaced mock arrays with typed empty arrays in all 7 affected files
- [x] Added empty state UI messages where lists render (tarefas, tarefas-org, admin.membros, apontamentos)
- [x] Removed fake notification badge dot and placeholder notification items in AppShell
- [x] Replaced hardcoded user name/org ("João Silva", "Acme Corp") with em-dash placeholders in AppShell sidebar
- [x] Replaced hardcoded profile initials "JS" and metric values with empty/dash placeholders in perfil.tsx
- [x] Dashboard heatmap now uses real current month/year via Intl.DateTimeFormat helpers
- [ ] Changes are NOT yet committed
- [ ] Real data integration (Supabase / API calls) has NOT been started

## Decisions Made This Session

- **Empty arrays over null/undefined**: All mock data arrays replaced with `[]` typed with explicit TypeScript types. This avoids runtime errors and lets the existing render logic handle the empty state gracefully.
- **Em-dash (`—`) as placeholder for user identity fields**: Used instead of removing elements entirely, to preserve layout structure while signaling missing data.
- **No data-fetching code added**: The scope was strictly cleanup of hardcoded data. Real integration is a separate task.
- **Dashboard helpers made dynamic**: `currentDateLabel()`, `currentMonthLabel()`, `daysInCurrentMonth()` functions added so calendar/heatmap reflects actual date instead of hardcoded "Junho".
- **Rejected**: Deleting the empty-state UI blocks entirely — kept them so the component structure survives until real data arrives.

## Files Touched

- `src/components/AppShell.tsx` — Removed fake notification dot, mock notification list items, and hardcoded user name/org; added "Nenhuma notificação" empty state.
- `src/routes/dashboard.tsx` — Replaced all mock recording/task/orgTask arrays with typed empty arrays; added dynamic date helpers for heatmap header and month calculation.
- `src/routes/tarefas.tsx` — Replaced mock task list with empty typed array; added "Nenhuma tarefa criada" empty state card.
- `src/routes/tarefas-org.tsx` — Replaced mock projects array with empty typed array; added Project type alias.
- `src/routes/apontamentos.tsx` — Replaced mock items array with empty typed array; changed initial selected state from `items[0].id` to `null`; guarded `current` lookup against null.
- `src/routes/perfil.tsx` — Replaced hardcoded metric values (hours, streak) with `—`; removed "JS" initials from avatar; cleared default values from form Fields; removed `defaultValue` prop from `Field` component signature.
- `src/routes/admin.membros.tsx` — Replaced mock members array with empty typed array; replaced hardcoded org ID "A3F9K2" with empty string; added `|| "—"` fallback in display.

## Next Steps

1. **Commit the current changes** with message describing the mock-data removal pass.
2. **Implement Supabase auth integration** — populate AppShell sidebar user name/org from `supabase.auth.getUser()` and the user's organization record.
3. **Wire dashboard data** — fetch real recordings/apontamentos for heatmap activity, priority tasks list, and org tasks list.
4. **Wire tarefas page** — fetch tasks from database filtered by current user.
5. **Wire apontamentos page** — fetch apontamento history from database, restore `selected` to first item once data loads.
6. **Wire tarefas-org page** — fetch projects and their tasks from database.
7. **Wire admin.membros page** — fetch org members list and real org ID from database.
8. **Wire perfil page** — fetch real hours/streak stats; load user name and email into form fields.

## Exact Resume Command

Tell the next session:

> "The mock data cleanup pass is done (7 files modified, not yet committed). Start by running `git diff HEAD` to confirm the changes, then commit them. After that, begin Supabase data integration, starting with AppShell user identity (auth session → sidebar user name and org)."

## Open Questions / Blockers

- **Supabase schema unknown**: The exact table names and column names for users, organizations, tasks, projects, and apontamentos have not been verified. Run `supabase list_tables` or check `/supabase/migrations/` before writing any queries.
- **Auth flow**: It is unclear whether the app uses Supabase Auth sessions directly or a custom user context/hook. Check for existing auth utilities before adding new ones.
- **Org ID format**: The `orgId` in `admin.membros.tsx` was hardcoded as "A3F9K2" (6-char uppercase). Confirm whether real org IDs follow this format or are UUIDs.
- **Recorder feature**: `dashboard.tsx` has a `Recorder` component with `recording`/`paused` state but no backend integration. Unclear if this is in scope for the current sprint.
