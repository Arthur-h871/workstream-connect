# Fase 2 — Redação de dados sensíveis.
# Este módulo será implementado em uma iteração futura usando modelos de visão
# computacional para detectar e ofuscar campos de senha, CPF, dados bancários, etc.


def redact(image_bytes: bytes) -> bytes:
    """Retorna a imagem sem modificações (fase 1 — sem redação)."""
    return image_bytes
